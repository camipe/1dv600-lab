(function () {
    "use strict";

    var LibraryDAO = require('../dao/LibraryDAO');

    var addBookToXML = ( xml, book ) => {
        return xml;
    };

    module.exports = function (data, callback) {
        callback();
    };

}());
